schema: 1
name: cursor
application:
  name: Cursor
  checked: Cursor subagent documentation as retrieved on 2026-09-25
  evidence:
    - https://cursor.com/docs/subagents
  notes:
    - Discovers Markdown agents with YAML frontmatter in ~/.cursor/agents/ and .cursor/agents/.
    - readonly blocks file edits and state-changing shell commands.
    - There is no per-agent tool allowlist and no documented skill preload field.
format: markdown
template: cursor.md.j2
output: generated/cursor/agents
noun: agent
model:
  key: model
  inherit: inherit
readonly:
  render: native
  key: readonly
tools:
  render: commented
  key: tools
  style: list
required_skills:
  render: commented
  key: skills
suggested_skills:
  render: instruction
